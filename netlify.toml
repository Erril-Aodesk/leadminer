[build]
  publish = "."

[functions]
  directory = "netlify/functions"

[functions.scrape]
  timeout = 26

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"